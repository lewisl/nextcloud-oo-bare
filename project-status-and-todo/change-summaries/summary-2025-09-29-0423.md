# Change Summary — $(date -u +"%Y-%m-%d %H:%M") UTC

- Updated `src/04_onlyoffice_install.sh` to restart nginx immediately after rendering the DocumentServer include, preventing the package install from leaving nginx in an unreloaded state that forced a second run of the script.
- The nginx restart is gated behind a config test (`nginx -t`) and only executes when the systemd unit exists, preserving idempotent reruns while eliminating the mid-script stop.
