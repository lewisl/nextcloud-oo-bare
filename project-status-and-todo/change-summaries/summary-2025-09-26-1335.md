# Summary – 2025-09-26 13:35 UTC

- Fixed OnlyOffice ↔ Nextcloud JWT integration by pinning Authorization headers and converter binary paths.
- Hardened nginx reverse proxy: forward the Authorization header, locked proto/port, and widened static asset caching to cover `.mjs`, `.wasm`, `webp`, and `avif` files.
- Added `text/javascript mjs;` to `/etc/nginx/mime.types` so browsers receive proper module MIME types.
- Baked the fixes into automation (`src/04_onlyoffice_install.sh`, `src/05_nginx_config.sh`, and the nginx templates) so reruns stay consistent.
- Replaced the dual-domain SSL helper with `src/06_ssl_setup.sh`, which issues/renews the docs.<domain> certificate (supports `--staging`, enables `certbot.timer`, and re-renders nginx automatically).
- Extended the nginx checklist and added `tests/nginx_smoke.sh` for loopback validation of headers, assets, and health endpoints.
- Introduced `src/07_integration_config.sh` to align OnlyOffice connector settings (`/onlyoffice/` public URL, JWT header/secret, internal URL) and run occ/nginx health checks.
- Replaced the destructive dual-domain teardown with a safer `src/99_uninstall.sh` (backups, database drops, optional package purge) and documented its checklist.
- Narrowed the `/apps/` rewrite to directories only, restoring direct delivery of Nextcloud Viewer's `.mjs` bundles and Text app assets.
- Refreshed `CURRENT_BEST_CONFIGURATIONS.md` and the troubleshooting playbook to reflect the working headers, converter settings, and nginx location regex updates.
- Verified OnlyOffice connector via `occ` and curl health checks; confirmed static asset endpoints now return 200 locally.
