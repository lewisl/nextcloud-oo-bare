# Summary – 2025-09-26 18:38 UTC

- Replaced the sample deployment parameters with a placeholder template (`configs/params.yaml`), updated system prep to copy it, and documented the template in `CURRENT_BEST_CONFIGURATIONS.md`.
- Defaulted the Nextcloud installer to 31.0.9 (overridable via `NEXTCLOUD_VERSION`), surfaced the deployed version, and warned admins to verify newer releases.
- Hardened the database bootstrap so the OnlyOffice role owns the `onlyoffice` schema; reruns now grant schema/table privileges idempotently.
- Stabilized OnlyOffice installation by pre-seeding logrotate/includes, forcing dpkg to keep managed configs, scrubbing the vendor example include, rebuilding `ds.conf` for 127.0.0.1:8080, and seeding the PostgreSQL schema post-install.
- Added noninteractive MIME-type editing in the nginx config script and tightened DocumentServer healthcheck + schema initialization sequencing.
- Improved uninstall resilience when params are missing and rewrote the MariaDB teardown with a heredoc to avoid shell escaping issues.
- Re-ran the full script chain (01→07) on a clean host; OnlyOffice health checks succeed, OCC reports JWT alignment, and nginx smoke tests pass (staging cert triggers expected `onlyoffice:documentserver --check` warning).
