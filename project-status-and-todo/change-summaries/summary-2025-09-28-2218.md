# Change Summary — 2025-09-28 22:18 UTC

- Updated `src/01_system_prep.sh` to auto-generate secure passwords for the Nextcloud and OnlyOffice databases, alongside the existing admin password / JWT secret handling.
- Added a root-only credential digest at `/root/nextcloud-onlyoffice-secrets.txt` so operators can retrieve generated passwords without editing YAML directly.
- Refreshed documentation (`docs/REBUILD_TEST_SERVER.md`, `CURRENT_BEST_CONFIGURATIONS.md`) to describe the new automatic secret generation workflow and post-run credential handling.
- Re-ran `01_system_prep.sh` to validate the new logic; confirmed the generated secrets persist in `/etc/nextcloud-onlyoffice/params.yaml`.
