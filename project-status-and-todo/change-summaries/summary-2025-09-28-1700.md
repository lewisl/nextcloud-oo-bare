# Change Summary — 2025-09-28 17:00 UTC

## Highlights
- Set Nextcloud `maintenance_window_start` to 02:00 and executed `occ maintenance:repair --include-expensive` to clear the remaining admin warnings about mimetypes/background jobs.
- Hardened nginx HTTPS template and live vhost with `X-Robots-Tag` and `X-Permitted-Cross-Domain-Policies` headers.
- Enabled environment variable access for PHP-FPM (`clear_env = no`) and documented the override in `configs/php/pool.d/www.conf`.
- Installed `php8.3-gmp` and `libmagickcore-6.q16-7-extra` so WebAuthn/SFTP and Imagick SVG rendering work out of the box.
- Refreshed `src/99_diagnostics.sh` run plus documentation to capture the new baselines.

## Follow-up
- Define SMTP defaults for automation scripts/runbooks.
- Fold the new package requirements and config tweaks into the deployment scripts before the next clean-host test run.
