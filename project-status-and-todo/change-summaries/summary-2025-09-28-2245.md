# Change Summary — 2025-09-28 22:45 UTC

- Added CLI argument parsing to `src/01_system_prep.sh` (`-d`, `-a`, `-m`) so operators supply the base domain and contact emails when running the system prep script.
- Script now writes the CLI values into `/etc/nextcloud-onlyoffice/params.yaml` before generating secrets, enabling a single-pass automated run.
- Refreshed `docs/REBUILD_TEST_SERVER.md` and `docs/QUICK_START.md` to document the new invocation pattern and required inputs; updated `CURRENT_BEST_CONFIGURATIONS.md` accordingly.
- Re-ran `01_system_prep.sh` with the new flags to confirm parameter population and successful idempotent execution.
