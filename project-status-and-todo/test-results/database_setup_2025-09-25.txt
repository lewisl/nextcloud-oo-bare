# 02_database_setup.sh run (2025-09-25)

Command: sudo ./tests/run_with_guard.sh ./src/02_database_setup.sh

## MariaDB diagnostics
- `mysql -u ncuser -p***** -e "SELECT 1;" nextcloud` → returns 1
- Grants:
$(mysql -u root -e "SHOW GRANTS FOR 'ncuser'@'localhost';")

## PostgreSQL diagnostics
- `PGPASSWORD=*** psql -h localhost -U oouser -d onlyoffice -c "SELECT 1;"` → returns 1
- Tables (`sudo -u postgres psql -d onlyoffice -c '\dt'`):
$(sudo -u postgres psql -d onlyoffice -c '\dt')

## pg_hba.conf check
$(grep -n "127.0.0.1/32" /etc/postgresql/*/main/pg_hba.conf)
$(grep -n "::1/128" /etc/postgresql/*/main/pg_hba.conf)

## Notes
- Script completed idempotently; databases/users already present.
- Maintenance mode remained disabled after guard cleanup.
