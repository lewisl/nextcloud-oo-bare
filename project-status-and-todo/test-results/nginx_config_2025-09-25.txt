# 05_nginx_config.sh run (2025-09-25)

Command: sudo ./tests/run_with_guard.sh ./src/05_nginx_config.sh

## nginx -t
nginx: the configuration file /etc/nginx/nginx.conf syntax is ok
nginx: configuration file /etc/nginx/nginx.conf test is successful

## Service status
$(systemctl is-active nginx)

## curl probes
- status.php: $(curl -sk https://docs.test-collab-site.com/status.php)
- onlyoffice healthcheck: $(curl -sk https://docs.test-collab-site.com/onlyoffice/healthcheck)

## Proxy stanza
$(sudo grep -n "proxy_pass" /etc/nginx/sites-available/docs.test-collab-site.com.conf)

## Notes
- Legacy `onlyoffice.<domain>` site removed.
- Template rendered with existing Let's Encrypt certificates; HTTP-only fallback not exercised.
- SSL stapling warnings persist due to OCSP responder absence (expected with current cert chain).
